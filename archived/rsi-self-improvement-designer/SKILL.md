---
name: rsi-self-improvement-designer
description: 基于用户实际项目，套用 RSI 五级自主权框架与环境-数据-模型四步闭环，设计可落地的 AI 自改进工作流方案。当用户希望给自己的代码库或 AI 工具加入"试错→验证→经验沉淀→自我改进"闭环、评估某工作流的 AI 自主权成熟度（L1–L5）、或把 RSI 方法论落到具体工程任务时，使用此 skill。
version: 1.0.0
agent_created: true
---

# RSI 自改进工作流设计器

把 RSI（递归式自我改进）从概念变成用户项目里可运行的闭环。核心转变：从"人类手动调参提能力"改为"AI 闭环试错 → 验证 → 沉淀经验 → 改进自己"。

设计前若需回顾完整概念、指标或产业数据，引用 `rsi-knowledge` skill；经验沉淀的落地实现复用 `wikiskill` skill（跨平台、带门控验证）。详见 `references/design-playbook.md`。

## 核心框架速查（设计时必须对齐）

### 五级自主权（人类退出 / AI 接管 的梯度）
| 级别 | 名称 | 人类负责 | AI 闭环接管 |
|---|---|---|---|
| L1 | 执行自主 | 设计任务、环境、更新策略 | 执行任务、更新 |
| L2 | 策略自主 | 设计任务、环境 | 设计更新策略（策略持久化） |
| L3 | 经验获取自主 | 设计环境 | 规划经验获取、执行、设计更新策略（经验设计持久化） |
| L4 | 环境适应自主 | 设定边界 | 与环境交互、设计更新策略（环境适应持久化） |
| L5 | 递归继承自主 | 设定边界 | 改进"改进系统本身"（系统设计持久化） |

### 环境-数据-模型四步闭环
1. **重构环境**：把原始工作区还原成可供 Agent 训练/评估的环境
2. **暴露缺口**：借新环境发现真实能力缺口，生成训练数据
3. **再训练/沉淀**：用生成数据训练任务模型；经验沉淀建议复用 `wikiskill` 的 `.wiki/` 三层架构（Raw→Wiki→Skill，带验证-回滚），天然跨平台
4. **迭代环境**：用更强模型推动下一轮环境迭代（形成闭环）

### 四大评估追问（避免"一次性刷榜"式伪改进）
- **可衡量**：独立评估下能否复现？
- **可保持**：改进能否持久不退化？
- **可迁移**：能否跨任务/领域复用？
- **可回滚**：失效时能否安全撤回？

### HCI 一句话
`HCI = (当前得分 − 基准得分) ÷ (100 − 基准得分) × 100`。标准化考试类能力快到顶，真实任务执行（软件工程 52.6 / 工具调用 Agent 39.9）缺口最大 = RSI 红利最高。

## 设计工作流（五步，详见 references/design-playbook.md）
1. **定位现状**：对照 L1–L5 判定当前级别，定下一步目标
2. **选落地切口**：按"反馈成本 / 可验证性 / 部署约束"三维选最优任务
3. **设计闭环**：套四步闭环，落到具体工程动作
4. **定义验收**：用四大追问为每个改进设独立验证与回滚
5. **输出方案**：按模板产出结构化文档

## 关键原则
- 只靠自评分宣称进步无效，必须在独立评估下确认
- L1–L4 在既定机制下提能力，L5 才可能触及有效递归
- 最优切口 = 反馈廉价、验证明确、部署链路短（软件工程 / 工具型 Agent 天然适合）
- 经验沉淀优先复用 `wikiskill`（`.wiki/` 与平台无关、可随项目 Git 跨机器迁移、带门控验证）；方案保持平台中立，不锁定特定 agent
- 人类始终把关总体目标与安全边界
